# 하네스 회고 로그

`retrospective` 스킬이 매 프로젝트 빌드 후(또는 수동 호출 시) 이 파일에 회고 항목을 append 한다.
각 항목은 그 시점에 발견한 신호와, 그에 따라 하네스/메모리에 반영한 변경을 기록한다.
이 로그의 목적은 **진화 궤적 추적**과 **퇴행 방지**다 — 과거에 내린 결정을 모르고 되돌리지 않도록 한다.

형식:
## {날짜} — {프로젝트명}
- 신호: {무엇을 관찰했나}
- 일반화: {원리 수준 교훈}
- 반영: {수정한 파일/메모리}
- 보존: {건드리지 않기로 한 좋은 패턴}

---

## 2026-06-14 — 하네스 초기 구축
- 신호: (초기 구축, 회고 대상 없음)
- 반영: 에이전트 7 + 스킬 6 + retrospective 스킬 + CLAUDE.md 포인터 생성
- 보존: 하이브리드 실행 모드, REQ-ID 기반 TDD 게이트, QA 경계면 교차 검증

## 2026-06-14 — 멀티유저 Todo 앱 (Kotlin+Spring+Postgres / React+TS+Vite)
- 신호:
  - (A) 여러 에이전트가 같은 `todo/backend`에서 `./gradlew test` 동시 실행 → 빌드 락/데몬 충돌로 **가짜 빌드 실패**. 리더가 즉석 직렬화로 해소.
  - (B) 사용자가 빌드 도중 **토큰 만료 대비 자동 재개**를 명시 요청 — 내장 체크포인트/재개 설계 부재.
  - (C) 팀 통신에 ack-only/예의성 메시지 다수 → 리더 idle 재진입 churn.
  - (D) OBS-1(catch-all이 프레임워크 4xx를 500으로 마스킹), OBS-2(타임스탬프 로컬 오프셋 누출), 부수: Vite 스캐폴드 index.css가 디자인 토큰과 충돌.
- 일반화/반영:
  - A → `architecture-and-delegation`에 "빌드/테스트 실행 프로토콜"(게이트 풀빌드 시니어 단독 1회, 스코프 테스트, --no-daemon) + `build-project` Phase 4 직렬화 노트 + 품질 게이트.
  - B → `build-project` "재개 체크포인트" 섹션(RESUME.md + 메모리 포인터 + 멱등 cron/SessionStart 훅 + BUILD_COMPLETE 마커) + Phase 6 트리거 정리 + 리더 독립 검증 단계. 메모리 `auto-resume-long-builds` 저장.
  - C → 5개 빌드 에이전트 정의 + build-project Phase 3 프롬프트에 "통신 규율".
  - D → `architecture-and-delegation` §4 "계약 하드닝 체크리스트"(날짜 포맷+타임존, catch-all 4xx 비마스킹) + §3 스캐폴드 스타일 정리 노트.
- 보존: 계약 우선(구현 전 양측 §3 고정)→경계면 0버그, 점진 QA, 단계적 팀 스폰(시니어 먼저), TDD 게이트 + 리더 독립 검증, QA의 OBS 선제 캐치.
- 퇴행 점검: 초기 구축의 보존 항목(하이브리드 모드·TDD 게이트·경계면 교차검증)과 충돌 없음 — 모두 강화 방향.

## 2026-06-14 — Todo 앱 라이브 테스트: CORS 403 / 실제 E2E 부재 (추가 회고)
- 신호: "QA 4라운드 통과 + 빌드 완료" 선언 후, 사용자가 실제 브라우저(cloudflared 터널)로 가입하자 **403 "Invalid CORS request"** → 프론트 "일시적 오류". 근본: 백엔드 CORS allowlist가 `localhost:5173/3000`만 허용, 터널 origin 거부.
- 왜 안 걸렸나: MSW(가짜 네트워크) + MockMvc(서블릿/CORS 필터 우회) + 계약-읽기 QA + curl(Origin 미전송)이 **전부 브라우저 실제 HTTP 의미를 우회**. 진짜 브라우저↔진짜 백엔드 E2E가 워크플로우에 없었고, 같은 날 추가한 "리더 독립검증"도 목 스위트+curl이라 동일한 사각.
- 일반화: 브라우저 프론트 + 별도 백엔드는 목/슬라이스/계약읽기/curl이 모두 그린이어도 CORS·프리플라이트·헤더/쿠키·리다이렉트·mixed content에서 깨질 수 있다 → **완료 선언 전 실제 브라우저↔실제 백엔드 1회 E2E 스모크 필수. curl ≠ 브라우저.**
- 반영: `qa-verification`에 "라이브 E2E 스모크" 필수 섹션 + "목 그린 ≠ 실제 통합" 원칙 / `build-project` Phase 6 리더 독립검증에 실제 앱 E2E 추가 / `architecture-and-delegation` 계약 하드닝에 cross-origin/CORS 항목. 메모리 `verify-web-apps-in-real-browser` 저장.
- 수정: dev fix로 Vite 프록시가 백엔드 전달 시 Origin 헤더 제거(`vite.config.ts`) → 터널 403→201 확인(헤드리스 CDP 재현). 백엔드 prod CORS는 의도대로 유지.
- 퇴행 점검: 오늘 추가한 "리더 독립검증(테스트 실행)"을 되돌리지 않고 **정제**(테스트 실행 + 실제 앱 E2E)함. 충돌 없음.

## 2026-06-15 — 지뢰찾기(Minesweeper) 빌드: 계약 협상 교착 + 프리플라이트
- 보존(검증됨): **todo 회고 개선이 이번에 실제로 작동**. CORS를 처음부터 올바르게 설계(상대 baseURL + Vite 프록시 + allowlist) → 라이브에서 프리플라이트 200·ACAO 확인, 403 회귀 0. 빌드 직렬화·계약 하드닝(UTC Z·4xx)·**라이브 E2E 스모크로 anti-cheat 바이트레벨 실증**(진행 중 mine 노출 0). 자가발전 루프가 효과를 냄.
- 신호:
  - (계약 교착) 두 시니어가 **상호 양보로 계약을 교차(swap)** → leaderboard 래핑/best 경로가 2회 어긋남. 협상 중 백엔드 문서가 왔다 갔다 해 시점별로 다르게 읽힘. 리더의 메시지 결정조차 stale 문서 재읽기로 뒤집힘 → 메시지 다수 왕복 + 리더 시간 소모. 리더가 "백엔드 파일 = 단일 소스"로 **파일에 동결**해서야 종료.
  - (프리플라이트) Docker 데몬 DOWN을 backend-senior가 게임엔진 통합 단계에서 블로커로 만남 → 리더가 사후 기동.
- 일반화/반영:
  - 계약 → `architecture-and-delegation §4`: **계약 단일 소스=백엔드 파일 §3 + 소유자=백엔드 시니어, 프론트는 제안만, 양쪽 동시 편집/상호 양보 금지, 2회 교차 시 리더가 파일에 동결**(메시지 아님). `build-project` 에러표에 "계약 협상 교착" 행 추가.
  - 프리플라이트 → `build-project Phase 3`: 팀 빌드 전 **런타임 의존 프리플라이트**(Docker up·포트·툴 버전) 선제 점검.
- 퇴행 점검: 기존 "계약 우선·경계면 교차검증·라이브 E2E·빌드 직렬화"를 강화만 함. 충돌 없음. (메모리: 새 사용자 취향 신호 없어 추가 안 함.)

## 2026-06-15 — 지뢰찾기 라이브 테스트: UI 완전 미스타일이 게이트 통과 (추가 회고)
- 신호: 사용자가 폰에서 "UI 엄청 구린데 반응형 안 된 거냐". 확인 결과 **컴포넌트 CSS가 아예 없었음**(global.css 36줄 리셋 + tokens.css 변수만; `.auth-form/.btn/.board/.cell/.control-bar` 등 규칙 0, 미디어쿼리 0). 컴포넌트는 className을 쓰는데 대응 CSS가 없어 **브라우저 기본 폼**으로 렌더. 그런데 프론트 145 테스트 + build + 라이브 E2E(동작/네트워크)는 **전부 그린** — 시각 품질을 아무도 검증 안 함.
- 일반화(사용자 통찰로 심화): 그동안 QA는 **기능 충실도**(REQ AC·상태 존재)만 봤고 **디자인 충실도(PD의 02_design대로 렌더됐는지)는 한 번도 검증 안 함**. 자동 테스트·행위 E2E는 시각을 안 본다. "토큰 정의 = 스타일 완료" 아님 — 사용 className에 대응 CSS 규칙 필요. **완료 전 02_design을 펴놓고 스크린샷(모바일+데스크톱)을 화면별로 대조한다.**
- 반영: `qa-verification`에 "**시각/디자인 충실도 점검**(02_design 화면별 스크린샷 대조 — 레이아웃·토큰·상태·시각규칙·반응형, className↔CSS 대응; 고충실도 시 PD 재리뷰)" 추가. `build-project` Phase 6 리더 독립검증에 스크린샷 시각 확인 추가. `architecture-and-delegation §3`에 프론트 "스타일 완료 정의"(토큰 넘어 컴포넌트 CSS+반응형, 위임 완료 기준 포함). 메모리 `verify-web-apps-in-real-browser`에 시각/디자인 충실도 점검 추가.
- 수정(이번 빌드): 토큰 기반 반응형 `components.css` 신규 작성 + global.css import → 헤드리스 CDP 스크린샷으로 모바일/데스크톱 정상 확인.
- 퇴행 점검: todo 회고의 "라이브 E2E 스모크"를 되돌리지 않고 **시각 차원까지 확장**(동작 + 시각). 충돌 없음.

## 2026-06-15 — (수동 개선, 대화) 아키텍처 결정 기준 + ADR
- 신호: 사용자 질문 "시니어들은 어떤 기준으로 아키텍처를 결정해?" → 점검 결과, 하네스가 명시한 기준은 스택·테스트용이성·위임 위주로 얇고, 코드레벨 아키텍처(레이어드/헥사고날/모듈경계/상태관리) 선택은 사실상 시니어(모델) 판단 + "관례 기본 + 사후 근거". 대안 비교·트레이드오프 기록 강제가 없었음.
- 일반화: 트레이드오프가 실재하는 결정은 "익숙한 기본"이 아니라 **프로젝트의 힘(테스트용이성·단순성·변경가능성·위임적합성·되돌림)에 비춰 대안과 비교**해 정하고, 그 과정을 산출물에 남겨야 한다(경량 ADR).
- 반영: `architecture-and-delegation`에 §3.5(결정 기준 + 경량 ADR) + 출력 템플릿 2.5(ADR 블록) + 품질 게이트 1줄. 과용 방지("트레이드오프 실재한 것만") 명시.
- 보존: TDD 강제가 구조를 미는 forcing function(순수 도메인 분리)·위임 경계 기준은 유지, 그 위에 명시적 대안 비교를 더함.

## 2026-06-16 — (수동 개선, 대화) 디자이너 시각화 + 자가 검증
- 신호: 사용자 질문 "디자이너가 어떻게 디자인해?" → 점검 결과, PD는 **텍스트(마크다운) 명세만** 산출하고 시각 목업/렌더가 없어, 자기 디자인을 눈으로 검증하지도, 대조할 시각 기준물을 남기지도 않았음. 이게 디자인 충실도 갭(미스타일 UI 통과)의 근원.
- 일반화: 디자인은 "행복 경로 텍스트"에 그치면 안 되고, **시각 기준물을 설계 단계에서 만들어 자가 검증**해야 한다(설계자가 자기 산출물을 본다). 그래야 프론트가 구현 타깃을 갖고, QA·리더가 실제 앱을 대조할 기준이 생긴다.
- 반영: `product-design`에 §7 와이어프레임(필수) + §8 HTML/CSS 프로토타입 제작→헤드리스 스크린샷 자가 검증(Chrome `--screenshot`, 모바일+데스크톱) + 출력 템플릿/게이트. `product-designer` 에이전트 역할에 추가. `build-project` Phase 2(프로토타입 산출·점검), `qa-verification` 시각 충실도 대조 기준에 프로토타입 스크린샷 추가.
- 보존: 모든 상태 정의·REQ 매핑·구체 토큰 규율 유지, 그 위에 시각화+자가검증을 더함. (todo→실브라우저 동작검증, 지뢰찾기→실렌더 시각검증, 이번→설계단계 시각 기준물 — 시각 검증이 "사후"에서 "설계 시점"으로 당겨짐)

## 2026-06-17 — Shop 빌드 이어받아 종결 (중단된 빌드 재개 회고)
- 신호:
  - (#1 완료-상태 모호) 이전 세션 팀이 사망하기 직전 프론트 게이트+라이브 E2E+디자인 충실도를 끝냈으나, **세 상태 문서가 서로 다른 시점**을 가리킴 — RESUME(22:47 "J5만 진행중") vs QA리포트(22:49 "E2E/디자인 미수행") vs 커버리지 매트릭스(23:00 "E2E PASS·디자인 일치"). 매트릭스가 **리더 전용 단계(라이브 E2E·디자인 충실도)를 팀 자가실행 후 "PASS"로 기록**, BUILD_COMPLETE는 부재. 재개자가 "매트릭스가 다 됐다니 완료"로 오판할 위험.
  - (#2 dev 의존 기동 명령 부재) dev Postgres 컨테이너가 Docker 재시작으로 소실 + compose 없음 → 정확한 `docker run`을 application.yml에서 역설계.
- 일반화/반영:
  - #1 → `build-project` Phase 6에 **완료 인증 단일 소스**(빌드 완료 = 리더 독립검증 후 BUILD_COMPLETE.md를 쓴 시점에만 성립; 매트릭스/QA의 완료 문구·특히 라이브 E2E·디자인 충실도는 리더 인증 대기 주장이지 게이트 아님; 팀 자가검증은 "리더 인증 대기"로만 표기) + Phase 0에 **중단 빌드 이어받기** 분기(BUILD_COMPLETE 부재 = 미완, 상태 문서는 검증 대기 주장으로 보고 직접 실행으로 재조정 후에만 완료 선언).
  - #2 → `build-project` 재개 체크포인트: RESUME.md에 수동 기동 런타임 의존의 **정확한 기동 명령**까지 적기.
- 보존(검증됨): 재개-후-실행검증 규율이 완벽히 작동(팀 산출물이 실제 옳았지만 재실행 없이는 알 수 없었음 — 불신·검증의 정확한 가치). 팀 사망 시 리더 직접 종결(잔여가 검증 성격이라 재스폰 불필요). 이전 회고 개선(라이브 E2E CORS 회귀0·디자인 충실도 스크린샷 대조·빌드 직렬화 팬텀0)이 이번에 그대로 효과.
- 퇴행 점검: 2026-06-14 "리더 독립 검증"을 되돌리지 않고 *그것이 유일한 완료 인증임*을 명시(강화). 기존 재개 체크포인트(RESUME/메모리/BUILD_COMPLETE 멱등)와 충돌 없음. 메모리: 새 사용자 취향 신호 없어 추가 안 함.

## 2026-06-17 — (수동 구축) 프로젝트 지식 베이스 + recall-knowledge
- 신호: 빌드를 거듭하며 쌓이는 산출물·회고가 시간이 지나면 잊혀짐. 클로드가 과거 지식을 결정 시점에 재사용할 구조 부재.
- 일반화/반영: 파일시스템=단일 원본, 생성 색인(index-core)으로 클로드는 INDEX.md 한 파일 Read 후 드릴다운(서버 불필요·DB/ES 불필요 — "전부 파일 조회"엔 DB가 오히려 이중화·drift 비용). recall-knowledge 스킬로 결정 전 조회 강제 + architecture-and-delegation §2 연동 + MEMORY.md 포인터(memory/project-knowledge-base). 사람용은 의존성 0 Node 웹(서버 매 요청 재생성 → 스테일 없음, 본문검색=ripgrep). 실제 브라우저(CDP)로 마크다운·코드·검색·모바일 렌더 검증.
- 보존: 기존 회고 루프(retro/log.md)·메모리 이중저장 규율과 충돌 없음 — 산출물 카탈로그 레이어를 추가만 함. "실제 브라우저 검증" 규율을 사람용 웹에도 적용.

## 2026-06-18 — Shop v2 (셀러 멀티마켓플레이스 확장)
- 신호:
  - (#1 주니어 marginal) backend-junior 산출 0(전 구간 시니어 도메인/서비스 의존으로 블록), frontend-junior JF1 owner만(나머지 senior 흡수). 주니어 작업이 시니어 기반에 100% 의존 → 위임 병렬화 실패.
  - (#2 주니어 visibility 충돌 3회) 두 시니어 모두 자기 주니어를 "미스폰"으로 오인 → 스폰 요청/자가구현 시도 → 파일 충돌 위험 3회(backend-junior 흡수, FE "spawn"요청, FE "내가 직접"+junior 동시). frontend-junior는 동시쓰기 감지하고 무편집 대기=옳은 행동.
  - (#3 게이트 드리프트) frontend-senior "tsc 0/build OK" 보고가 리더 인증 시점엔 tsc 2에러(미사용 import, 최종 편집 후 드리프트). vitest만 봤으면 놓쳤을 것 — 리더 풀게이트 재실행이 포착.
- 일반화/반영:
  - #1/#2 → `build-project` Phase 3: **주니어 just-in-time 스폰**(기반준비된 독립 작업 배치 실재 시에만, 없으면 시니어 마무리·주니어 스폰 안 함) + **오케스트레이터 소유권 브로커링**(스폰 즉시 주니어 소유 task/파일 시니어에 통지). Phase 4 item5: 타인 owner task/파일 동시구현 금지. `agents/backend-senior·frontend-senior`: "주니어 스폰/생존 가정 금지, 미스폰 단정해 자가구현 금지, 타인 소유 비편집".
  - #3 → `architecture-and-delegation` 게이트 정의: 프론트 게이트=**tsc -b + vite build + vitest 셋 다**, 최종 편집 직후 1회 실행 보고(이전 그린 재사용 금지). `build-project` Phase 6: 리더 인증=풀게이트 재실행(테스트만 아님).
- 보존(검증됨): **완료 인증 단일소스(2026-06-17)가 즉시 효과**(tsc 드리프트 포착). 계약 §3 단일소스+리더 경계동결 브로커링(재체크아웃 가드·orders 형태→동시성테스트와 확정→FE 0충돌). 점진 QA가 blocker#1(sellerStatus 목≠실) 개발중 포착. 라이브 API 스모크(37)+디자인충실도 스크린샷 리더 인증.
- 퇴행 점검: 2026-06-14 5인 팀을 폐지 않고 주니어 스폰 *타이밍*+소유권 브로커링 정제(주니어 유지). 2026-06-17 완료인증을 게이트 정의(tsc+build+test)로 구체화·강화. 충돌 없음. 메모리: 새 사용자 취향 신호 없어 추가 안 함.

## 2026-06-18 — (사용자 요청, 대화) PM 엄밀 모드
- 신호: 사용자 질문 "PM이 더 만들어야 하는 산출물?" + "PRD와 차이?". 점검 결과 PM은 `01_requirements.md`(REQ+AC+범위) 하나만 산출 — 도메인 용어·불변식·상태기계·엣지 시나리오는 시니어 ADR/QA가 사후 보강하거나(shop서 재체크아웃 가드는 개발 중 frontend-senior가 늦게 제기→리더 동결 중재) 흩어져 있었음.
- 일반화: 요구사항의 엄밀성은 "기능 목록"이 아니라 **비즈니스 시맨틱**(용어집·불변식·상태전이·예시 시나리오)에서 나온다. 이걸 PM 단계에서 확정하면 시니어 추측·QA 사후보강·늦은 경계 질문이 앞단에서 닫힌다. 단 PD(시각)·시니어(기술 계약)와 *경쟁하는 두 번째 소스를 만들지 말 것* — PM은 비즈니스 의미만.
- 반영: `define-requirements`에 절차 6(엄밀 산출물) + 출력 템플릿 §3 용어집·§5 불변식(INV)·§6 상태전이표·§8 수용시나리오 + 게이트 체크. `agents/pm` 핵심역할 5 추가. `build-project` Phase1 산출물에 명시. 스케일링(단순 CRUD는 생략, 상태/동시성/금전/권한 도메인은 필수)으로 과대명세 방지.
- 보존: REQ-ID 추적성·검증가능 AC·범위 닫기는 유지, 그 위에 도메인 시맨틱을 더함. PM=비즈니스 의미, 스택/기술은 여전히 시니어.
- 퇴행 점검: 기존 define-requirements 구조(REQ 테이블·AC·범위·재실행 ID 보존)와 충돌 없음 — 섹션 추가. 메모리: 새 사용자 취향 신호 없어 추가 안 함(절차 개선은 하네스 파일에만).

## 2026-06-19 — Message Platform (1000 TPS · 자율 빌드)
- 맥락: 사용자 "나 잘거니까 알아서 잘 만들어놔"(AFK 자율 완성 위임). Kafka+Redis+PG, 멀티채널·멱등·재시도·서킷·DLQ·bulkhead·정산·대시보드. 자동재개(cron+SessionStart 훅) 무장.
- 신호:
  - (#1 팀 hang) 후반 아이들 데드락→완전 hang(backend-junior shutdown 무시, 35분+ 무활동). 리더가 전 에이전트 kill + 직접 백엔드 게이트 종결로만 복구.
  - (#2 Docker 크래시) 시니어+주니어 동시 gradle/Testcontainers(Kafka+PG+Redis ×2)가 Docker 데몬 크래시(2회 XML 팬텀 후 3회째 다운).
  - (#3 중복 컨트롤러) 두 작업스트림이 `/api/stats` 중복 매핑 → ambiguous mapping → 컨텍스트 실패 → 82 테스트 캐스케이드. 리더가 redundant 패키지 제거.
  - (#4 async 테스트 flake) StatsApiTest 카운트·SettlementApiTest ID충돌이 풀스위트서만 실패(격리선 통과). 동기 시드+시간윈도우로 해결.
  - (#5 Kotlin 중첩주석) `/api/x/**`의 `/*`가 KDoc 안에서 주석 열어 컴파일깨짐 3회.
- 반영(전부 승인): build-project 에러표 "팀 stall/hang→리더 takeover"; architecture-and-delegation 빌드프로토콜에 단일 테스트러너+Testcontainers reuse, 소유권에 엔드포인트당 단일소유자+중복매핑 점검; tdd-implementation 비동기 파이프라인 테스트 격리; agents/backend-senior·junior Kotlin 주석 한 줄; memory auto-resume-long-builds에 AFK 팀hang takeover.
- 보존(검증됨): **자동재개 cron이 stall 탐지 안전망으로 작동**, **완료인증 단일소스가 팀 거짓완료(중복컨트롤러·J5/J7 stub) 포착**, **PM 엄밀모드 INV/SC가 부하-검증 기준 제공**(1000 TPS서 무유실·과금정합 SQL 검증), 계약 단일소스+경계동결, recall-knowledge(shop prior-art 재사용), k6 부하 하니스.
- 결과: 백엔드 111 + 프론트 114 + **1000 TPS 부하 PASS(999/s·p99 8.94ms·0err·무유실·과금정합)** + 라이브 E2E + 디자인충실도. 전 게이트 리더 독립 검증.
- 퇴행 점검: 전부 기존(직렬화·완료인증·JIT주니어·계약단일소스·PM엄밀) 강화. 되돌림 없음.

## 2026-06-19 — (수동 개선) 지식 베이스 OKF 정합 + 개념 그래프
- 신호: 사용자가 Google Open Knowledge Format(OKF, 2026-06-13) 두 소스 제시. OKF = AI 에이전트용 지식을 "YAML frontmatter 마크다운 파일 디렉터리 + 마크다운 링크 그래프 + index.md 점진공개 + 정적 비주얼라이저"로 표준화. 우리 knowledge/ 설계가 OKF와 **독립 수렴**(파일=원본·이중가독·INDEX.md·웹뷰어·recall-knowledge=행동전 조회).
- 일반화/반영: 표준과 수렴했으니 OKF 어휘로 정렬. (A) 카드 본문 prose 참조를 실제 마크다운 링크로 → 개념 그래프. (B) frontmatter에 type·timestamp 추가 = OKF 호환(외부 OKF 도구 소비 가능, 미지 키는 parseCard가 무시하므로 비파괴). (C) 교차 프로젝트 재사용 패턴을 knowledge/patterns/*.md 개념 문서 6종으로 추출(내가 가장 자주 꺼내쓰는 게 프로젝트가 아니라 재사용 패턴이라 장기가치 최대). generator가 패턴 파싱+그래프 링크+OKF필드 보존·렌더, INDEX.md 최상단에 패턴 노출. 웹은 패턴 사이드바 + 본문 그래프 링크 클릭 내비 + frontmatter 표시 제거.
- 보존: DB·ES 없음·파일=원본·소비시 자동생성 규율 유지(확장만). 실제 브라우저(CDP) 검증 규율로 그래프 내비·패턴 렌더 확인. 테스트 16→20.
- 퇴행 점검: 기존 카드/생성기/스킬 비파괴 확장(기존 20테스트 그린). YAGNI로 보류: 디렉터리별 index.md, 그래프 시각화 UI, 별도 log.md(retro가 대체).

## 2026-06-19 — (수동 기능) 지식 베이스 채팅 (claude -p 재사용)
- 신호: 사용자가 "사이트에서 문서 내용을 질문하면 답하는, 이 세션처럼 이어지는 채팅" 요청.
- 일반화/반영: 별도 API 키 없이 기존 Claude Code 구독 auth를 child_process로 재사용(claude -p --output-format json). --system-prompt override로 경량화(8574→10토큰) + INDEX.md 임베드, read-only 툴(Read/Grep/Glob) 드릴다운, --resume로 대화 지속(프론트가 sessionId 메모리 보관, 리프레시=새 대화). 모델은 구독 할당량(달러 아님)을 태우므로 보조 채팅은 sonnet 기본. 터널 노출=할당량 소진 위험 → 선택적 CHAT_TOKEN.
- 보존: 의존성 0·파일=원본 규율 유지. 실제 spawn은 비용 때문에 자동 테스트 제외하고 라이브(curl+CDP)로 검증 — "실제 브라우저 검증" 규율 적용. 토큰/본문검증 게이트는 spawn 전 조기반환이라 단위테스트 가능.
